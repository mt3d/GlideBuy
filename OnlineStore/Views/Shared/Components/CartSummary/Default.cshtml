@using GlideBuy.Core.Infrastructure
@model IList<ShoppingCartItem>

<div class="">
	@if (Model.Count() > 0)
	{
		<small class="navbar-text">
			<b>Your cart:</b>
			@Model.Sum(x => x.Quantity) item(s)

			@*TODO: Use GetSubtotalAsnyc()*@
@* 			@Model.ComputeTotalValues().ToString("c")*@
		</small>
	}
	<a class="btn btn-sm btn-secondary navbar-btn" asp-controller="ShoppingCart" asp-action="Cart" asp-route-returnurl="@ViewContext.HttpContext.Request.PathAndQuery()">
		<i class="fa fa-shopping-cart"></i>
		</a>
</div>